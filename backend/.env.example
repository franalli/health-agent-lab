# Secrets + runtime config ONLY. Clinical thresholds never live here — they belong in
# config.py (versioned, auditable). Copy this file to `.env` (git-ignored) and fill in.

# Required for Mode 2 (LLM compose) and the input gate classifier.
# Mode 1 (deterministic) runs fully without it.
ANTHROPIC_API_KEY=

# SQLite database file — read by the app as HEALTH_DB_PATH. Optional: leave unset to use the local
# default backend/data/health.db, which is created (init_db) and seeded on startup, and by `make
# init-db` / `make seed`. Set it to target a specific path, e.g. a persistent disk on a paid hosted
# deploy (HEALTH_DB_PATH=/data/health.db); the free/ephemeral tier leaves it unset (ephemeral FS).
# Renamed from DATABASE_PATH — the code no longer reads that name, so rename a custom value if you set one.
# HEALTH_DB_PATH=

# Active dataset: the sub-folder of `backend/data/` to ingest. Each bundle (members.json,
# lab_panels.csv, eval_set.jsonl) lives in its own sub-folder, so new datasets can be added
# and ingested incrementally. Read from the environment by `preprocessing.datasets`;
# defaults to `training_data` when unset.
DATASET=training_data

# Optional: stream eval runs to the offline LangSmith trace sink (synthetic data only).
# Leave unset to keep the sink off; the local report stays canonical.
# LANGSMITH_API_KEY=
