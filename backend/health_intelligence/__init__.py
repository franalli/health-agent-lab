"""Health Intelligence Service — the serving library (SQLite -> answer).

Pure analysis + safety core plus the thin LLM language layer. Imports no web
framework; `api.py` adapts these functions to HTTP routes.
"""
